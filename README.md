# Alf

Alfredo CLI and Agent, runs in the VPS

## Installing

curl -L https://github.com/getalfredo/alf/releases/download/v0.0.3/task-runner-linux-x64 -o alf
chmod +x alf
sudo mv alf /usr/local/bin/alf
