This project is to make a lightweight task runner cli.

I want to point a filepath, that will be a yaml file, and it will run the tasks defined in that yml, similarly to a GitHub action, but much simpler.

Let's make a PRD for this so we can evolve and iterate the idea.
